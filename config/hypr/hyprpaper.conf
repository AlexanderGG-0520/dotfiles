# Sapphire Shell uses one wallpaper for every output. These are packaged
# Hyprland assets, so this configuration has no dependency on a user folder.
preload = /usr/share/hypr/wall0.png
wallpaper = ,/usr/share/hypr/wall0.png
